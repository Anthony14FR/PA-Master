# =============================================================================
# API Configuration
# =============================================================================

# URL de l'API Laravel backend
# Utilisé par le client API pour toutes les requêtes HTTP
# IMPORTANT: Doit être préfixé par NEXT_PUBLIC_ pour être accessible côté client
NEXT_PUBLIC_API_URL=http://localhost:8000

# =============================================================================
# Internationalization (i18n) Configuration
# =============================================================================

# Locale par défaut de l'application
# Utilisée comme fallback si aucune locale n'est détectée
NEXT_PUBLIC_DEFAULT_LOCALE=en

# Locales disponibles (séparées par des virgules)
# Définit les langues supportées par l'application
NEXT_PUBLIC_AVAILABLE_LOCALES=fr,en,it,de

# Mapping domaine → locale
# Format: domain:locale,domain:locale,...
# En mode développement (localhost), les redirections de domaine sont désactivées automatiquement
# Vous pouvez tester toutes les locales sur localhost sans être redirigé
NEXT_PUBLIC_DOMAIN_LOCALES=kennelo.fr:fr,kennelo.com:en,kennelo.it:it,kennelo.be:fr,kennelo.de:de

# Codes de locale complets pour les balises hreflang et OpenGraph
# Format: locale:code,locale:code,...
NEXT_PUBLIC_LOCALES_CODES=fr:fr-FR,en:en-US,it:it-IT,de:de-DE

# =============================================================================
# SEO & Analytics (optionnel)
# =============================================================================

# Code de vérification Google Search Console
# GOOGLE_SITE_VERIFICATION=your_verification_code_here