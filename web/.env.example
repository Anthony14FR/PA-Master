# =============================================================================
# API Configuration
# =============================================================================

# URL de l'API Laravel backend
# Utilisé par le client API pour toutes les requêtes HTTP
# IMPORTANT: Doit être préfixé par NEXT_PUBLIC_ pour être accessible côté client
NEXT_PUBLIC_API_URL=http://localhost:8000

# =============================================================================
# JWT Authentication Configuration
# =============================================================================

# Secret JWT utilisé pour vérifier la signature des tokens
# IMPORTANT: Doit être IDENTIQUE au JWT_SECRET du backend Laravel
# Utilisé côté serveur uniquement (middleware, API routes)
JWT_SECRET=

# Algorithme de signature JWT (HS256, HS384, HS512, RS256, etc.)
# IMPORTANT: Doit correspondre au JWT_ALGORITHM du backend Laravel
JWT_ALGORITHM=HS256

# Durée de vie de l'access token en secondes (1 heure par défaut)
JWT_ACCESS_TOKEN_TTL=3600

# Durée de vie du refresh token en secondes (30 jours par défaut)
JWT_REFRESH_TOKEN_TTL=2592000

# Marge de tolérance pour l'expiration des tokens en secondes
# Permet de compenser les décalages d'horloge entre serveurs
JWT_LEEWAY=60

# =============================================================================
# Cookie Configuration
# =============================================================================

# Domaine pour les cookies (permet le partage cross-domaine)
# Ex: .kennelo.com permet l'accès depuis kennelo.fr, kennelo.com, kennelo.it, etc.
# Laisser vide pour le domaine actuel uniquement
# IMPORTANT: Doit commencer par un point pour les sous-domaines
NEXT_PUBLIC_COOKIE_DOMAIN=

# =============================================================================
# GeoIP Configuration
# =============================================================================

# Chemin vers la base de données GeoLite2 Country de MaxMind
# Utilisé pour la géolocalisation par IP et la redirection automatique de domaine
GEOIP_DB_PATH=/var/geoip/GeoLite2-Country.mmdb